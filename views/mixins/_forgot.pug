mixin forgotForm()
  form.form(action="/account/forgot" method="POST")
    h2 I forgot my password!
    label(for="email") Email
    input(type="email" name="email")
    input.button.btn.btn-primary(type="submit" value="Send a Reset")
